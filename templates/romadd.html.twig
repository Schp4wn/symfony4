{% extends 'base.html.twig' %}

{% block main %}
    <h1>Add new shit:</h1>
    <div class="rom" id="forms">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}