{% extends 'base.html.twig' %}


{% block main %}
    <div class="container">
    <h1>Add new shit:</h1>
    <div class="system" id="forms">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
    </div>
{% endblock %}